# คู่มือติดตั้ง (ทำครั้งเดียว ประมาณ 10 นาที ฟรีทั้งหมด)

แอปนี้เป็นเว็บ static ธรรมดา (ไม่ต้อง build) + ใช้ **Supabase** (ฟรี) เป็นระบบหลังบ้าน
สำหรับ login, ฐานข้อมูล และดูเอกสารย้อนหลังจากทุกเครื่อง

---

## ขั้นที่ 1 — สร้างโปรเจกต์ Supabase (ฟรี)

1. เข้า <https://supabase.com> → สมัคร/ล็อกอิน (ใช้บัญชี GitHub หรืออีเมลก็ได้)
2. กด **New project**
   - ตั้งชื่อโปรเจกต์อะไรก็ได้ เช่น `quotation`
   - ตั้งรหัสผ่านฐานข้อมูล (จดเก็บไว้ แต่แอปไม่ได้ใช้ค่านี้)
   - Region เลือก **Southeast Asia (Singapore)** จะเร็วสุดสำหรับไทย
   - Plan เลือก **Free**
3. รอโปรเจกต์สร้างเสร็จ (~1-2 นาที)

## ขั้นที่ 2 — สร้างตารางฐานข้อมูล

1. เมนูซ้าย → **SQL Editor**
2. เปิดไฟล์ [`supabase/schema.sql`](supabase/schema.sql) ในโปรเจกต์นี้ คัดลอก **ทั้งไฟล์**
3. วางลงใน SQL Editor แล้วกด **Run**
4. ขึ้น "Success. No rows returned" = เรียบร้อย

## ขั้นที่ 3 — สร้างบัญชีผู้ใช้ของคุณ (ระบบส่วนตัว ใช้คนเดียว)

แอปนี้ไม่มีหน้าสมัครสมาชิก — สร้างบัญชีเองใน Supabase ครั้งเดียว:

1. เมนูซ้าย → **Authentication** → **Users** → กด **Add user → Create new user**
2. กรอกอีเมล + รหัสผ่าน แล้วติ๊ก **Auto Confirm User** → Create
3. ปิดรับสมัครจากคนนอก: เมนู **Authentication → Sign In / Providers**
   → ปิดสวิตช์ **Allow new users to sign up** → Save

## ขั้นที่ 4 — เชื่อมแอปเข้ากับ Supabase

1. เมนูซ้าย → **Settings** (ไอคอนเฟือง) → **API**
2. คัดลอก 2 ค่า:
   - **Project URL** (หน้าตาแบบ `https://xxxx.supabase.co`)
   - **anon public** key (ตัวอักษรยาวๆ)
3. เปิดไฟล์ [`js/config.js`](js/config.js) ในโปรเจกต์นี้ แล้วใส่ค่าทั้งสอง:

```js
window.APP_CONFIG = {
  SUPABASE_URL: "https://xxxx.supabase.co",
  SUPABASE_ANON_KEY: "eyJhbGciOi....",
};
```

> anon key เป็น key ฝั่งหน้าเว็บ เปิดเผยได้ ไม่ใช่ความลับ — ความปลอดภัยของข้อมูล
> คุมด้วย Row Level Security ที่ตั้งไว้ใน schema แล้ว (แต่ละบัญชีเห็นเฉพาะข้อมูลตัวเอง)

## ขั้นที่ 5 — เอาเว็บขึ้นออนไลน์ (เลือกวิธีเดียว ฟรีทุกวิธี)

เพราะเป็นเว็บ static ล้วน ใช้บริการโฮสต์ฟรีอะไรก็ได้:

### วิธีที่ง่ายสุด: Netlify Drop
1. เข้า <https://app.netlify.com/drop>
2. ลากโฟลเดอร์โปรเจกต์นี้ทั้งโฟลเดอร์ไปวาง → ได้ URL ใช้งานทันที

### หรือ: Vercel / Netlify ผูกกับ GitHub (อัปเดตอัตโนมัติเมื่อโค้ดเปลี่ยน)
1. เข้า <https://vercel.com> → Add New Project → เลือก repo นี้
2. Framework Preset เลือก **Other**, ไม่ต้องตั้งค่า Build (ปล่อยว่าง) → Deploy

### หรือ: GitHub Pages (repo ต้องเป็น public)
1. ที่ repo → Settings → Pages → Source: Deploy from a branch → เลือก branch หลัก, โฟลเดอร์ `/ (root)` → Save

## ขั้นที่ 6 — เริ่มใช้งาน

1. เปิดเว็บจากมือถือ → เข้าสู่ระบบด้วยอีเมล+รหัสผ่านที่สร้างไว้ในขั้นที่ 3
2. ไปที่ **ตั้งค่า (⚙️) → เพิ่มบริษัท** กรอกชื่อ ที่อยู่ เลขผู้เสียภาษี บัญชีธนาคาร
   และอัปโหลด โลโก้ / ลายเซ็น / ตราประทับ (แนะนำ PNG พื้นหลังโปร่งใส)
3. กลับหน้าแรก → กด **+ สร้างใบเสนอราคา** → กรอก → **⬇️ ดาวน์โหลด PDF** ได้เลย

💡 เคล็ดลับมือถือ: เปิดเว็บใน Safari/Chrome แล้วกด **Add to Home Screen / เพิ่มลงหน้าจอโฮม**
จะได้ไอคอนเหมือนแอปจริง เปิดใช้ได้เร็ว

---

## คำถามที่พบบ่อย

**ฟรีจริงไหม?** — Supabase Free tier ให้ฐานข้อมูล 500MB เพียงพอกับเอกสารหลายหมื่นใบ
ส่วนโฮสต์เว็บ (Netlify/Vercel/GitHub Pages) ฟรีสำหรับการใช้งานระดับนี้
ข้อจำกัดเดียวของ Supabase Free คือโปรเจกต์จะ pause ถ้าไม่มีการใช้งานเลย ~1 สัปดาห์
เข้า dashboard กด Restore ได้ (ข้อมูลไม่หาย)

**ใช้หลายเครื่องได้ไหม?** — ได้ ล็อกอินด้วยบัญชีเดียวกัน ข้อมูลตรงกันทุกเครื่อง

**ลืมรหัสผ่าน?** — เข้า Supabase dashboard → Authentication → Users → เลือก user → Send password recovery
(หรือลบ user แล้วสมัครใหม่ ถ้ายังไม่มีข้อมูลสำคัญ)
